version: '3'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/your_database
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
